{
  "version": "1.3.0",
  "name": "webfont-lxgw-wenkai-monorepo",
  "description": "monorepo for lxgw-wenkai-webfont",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "python -m update",
    "build:package": "python -m update --package",
    "release": "npx -y -p @qiwi/semrel-toolkit multi-semrel"
  }
}
